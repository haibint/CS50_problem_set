#define _XOPEN_SOURCE
#include <unistd.h>
#include <stdio.h>
#include <cs50.h>
#include <string.h>
#include <stdlib.h>

int main(int argc, string argv[2])
{
    if (argc == 2)
    {
        string input = argv[1];
        do // create a string here.
        {
            char result[];
            for (i=0; i< 4; i++)  // count out how many characters it should be.
            {

            }
        }
        while (result != input);
        printf("%s\n", input);  // print the clear password here.
        return 0;
    }
    printf("You should only put one command line arguement.\n");
    return 1;
}


string guess(void)
{

}